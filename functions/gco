#!/bin/zsh
#
# gco — smart git checkout
#
# 1. Try local checkout first
# 2. If branch doesn't exist locally, check for a remote tracking branch and pull it
# 3. If no remote branch either, prompt to create it

local output
output=$(git checkout "$@" 2>&1)
local rc=$?

if [[ $rc -eq 0 ]]; then
  [[ -n "$output" ]] && echo "$output"
  return 0
fi

# Check if failure is "branch not found"
if [[ "$output" == *"did not match any"* ]] || \
   [[ "$output" == *"not a commit"* ]] || \
   [[ "$output" == *"invalid reference"* ]]; then

  # $@ could include flags; grab just the branch name (last arg)
  local branch="${@[-1]}"

  # Check local remote-tracking refs first
  local remote_ref
  remote_ref=$(git branch -r --list "*/$branch" 2>/dev/null | head -1 | tr -d ' ')

  if [[ -n "$remote_ref" ]]; then
    echo "gco: '$branch' found on remote, checking out and tracking $remote_ref"
    git checkout --track "$remote_ref"
    return $?
  fi

  # Local refs may be stale — offer to fetch or create
  echo -n "gco: '$branch' not found locally. [r]emote fetch / [c]reate / anything else cancels: "
  read -r reply
  if [[ "$reply" == [rR] ]]; then
    git fetch --all --quiet
    remote_ref=$(git branch -r --list "*/$branch" 2>/dev/null | head -1 | tr -d ' ')
    if [[ -n "$remote_ref" ]]; then
      echo "gco: '$branch' found on remote, checking out and tracking $remote_ref"
      git checkout --track "$remote_ref"
      return $?
    fi
    # Not on remote either — offer to create
    echo -n "gco: '$branch' not found on any remote. Create it? [y/n] "
    read -r reply
    if [[ "$reply" == [yY] ]]; then
      git checkout -b "$@"
      return $?
    fi
  elif [[ "$reply" == [cC] ]]; then
    git checkout -b "$@"
    return $?
  fi
  return 1
fi

# Some other error — just show it
echo "$output" >&2
return $rc
